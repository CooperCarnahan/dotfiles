# Docked s2idle on this platform burns power without reaching s0i3, so keep
# the machine running when external displays are connected and only suspend
# when undocked. Idle-timer lock (via hypridle) still fires either way.

[Login]
HandleLidSwitch=suspend
HandleLidSwitchExternalPower=suspend
HandleLidSwitchDocked=ignore
